# Python bytecode generated by running the offline test harness (tests/run.py).
__pycache__/
*.pyc

# workspace.py's own worktrees (architecture.md §6) — this repo doubles as a consuming repo when
# workspace.py's manual smoke (`ensure --read`, `gc`) is run against it directly.
.worktrees/
