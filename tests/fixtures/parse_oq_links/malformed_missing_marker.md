## Summary
Adversarial mutation: an `## Open questions` heading present but without the required
`<!-- open-question-links:v1 -->` marker as the section's first content line — per
open-question-links.md, "readers locate the registry with a `startswith` match on that line",
so a section without it is not this registry at all and must parse as empty, not malformed.

## Open questions
- OQ: `OQ-23` (docs/prd.md §9 Open questions) — gates: whether guest checkout is supported
  — disposition: scoped-out — question: #302 — audience: audience:business
