## Summary
Adversarial mutation: an annotation-shaped parenthetical using a prefix word outside the closed
set (`fixed by` instead of `closed by`).

## Definition of done
- [x] The crash no longer reproduces on cold start (fixed by phase 1, commit abc1234)
