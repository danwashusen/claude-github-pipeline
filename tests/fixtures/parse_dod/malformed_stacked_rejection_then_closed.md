## Summary
Adversarial mutation: a second stacking shape — an evaluator-rejection annotation with a
`closed by` annotation appended after it (rather than the rejection being overwritten as the
invariant requires).

## Definition of done
- [ ] The export completes for files over 50MB (resolver claimed phase 3, commit 1a2b3c4; evaluator rejected: streaming path still buffers the whole file in memory) (closed by phase 4, commit 2b3c4d5)
